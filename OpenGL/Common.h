#pragma once
#include <array>
#include <vector>
#include <math.h>
#include <functional>
#include <algorithm>
#include <memory>
#include "MathUtils.h"


#define Min(a, b) (((a) < (b)) ? (a) : (b))
#define Max(a, b) (((a) > (b)) ? (a) : (b))

