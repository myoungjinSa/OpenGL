#version 400 core

//Ãâ·Â
layout (location = 0) out vec4 color;

uniform sampler2D shaderTexture;

in VS_OUT
{
	vec2 uv;
	vec3 color;
} fs_in;

void main(void){
	vec4 textureColor = texture(shaderTexture, fs_in.uv);
	color = vec4(fs_in.color, 1.0) * textureColor;
}
